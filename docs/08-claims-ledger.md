# Claims ledger

Every proposed claim must identify its closest prior art, supporting experiment, falsifier, and current status.

| ID | Proposed claim | Closest prior art | Required evidence | Falsifier | Status |
|---|---|---|---|---|---|
| C1 | Structural defects predict task damage during typed conversions | RTD, RTD-AE, SRTD | Held-out correlation and intervention tests beyond reconstruction baselines | No incremental predictive value | **Untested.** Four corrected reports evaluate fixed-expert clean/corrupted embeddings, not a translator or learned map. Each kind has 13 complete blocks, 65 batch observations, and 306 unique examples. Eleven of 12 within-checkpoint block-bootstrap intervals include zero; the full/edge-cochain interval excludes zero, but its within-block permutation p=0.115 and no multiplicity correction was made. Every audit-designed paired ablation contrast against task-only includes zero (p≥0.324); these contrasts were not preregistered. These checkpoint-conditional diagnostics do not test damage during typed conversion. |
| C2 | Map-aware cone loss improves a learned graph/cell/sheaf translator | RTD-AE, differentiable liftings | Matched-compute ablations across synthetic regimes and a real task | Task/cycle-only model matches it | **Direct claim untested.** Historical ablations show that reconstruction, local-consistency, and H0-distance surrogates did not improve task metrics; they lacked a learned-map cone objective and consumed target-view structure. Target-held-out structure-reconstruction objectives and a separate exact two-term map demo now exist, but the current generator makes the held-out targets unidentifiable from graph inputs. No matched downstream cone ablation has been completed. |
| C3 | A cost-aware router specializes by structural regime | MoE routing, learned topological liftings | Regime-conditioned route analysis and fixed/random/oracle comparisons | Route collapse or shortcut behavior | **Scoped routing endpoint supported under the v2 protocol.** Across five valid seeds, hard-minus-best-fixed was +0.1098 (SD 0.0117; Student-t 95% CI [0.0953, 0.1243]); all margins were positive. This is the historical regime-distilled router: training used privileged regime labels and inference used graph, active-face, and sheaf-transport summaries. It is not graph-only conversion. The campaign is protocol-aligned, not pristine preregistration, because an aborted pre-commit seed-20260906 attempt exposed validation metrics under different code. With n=5, assumptions are uncheckable and the exact two-sided sign-test sensitivity is p=0.0625. |
| C4 | Direct cone loss supplies information not contained in RTD | RTD/SRTD | Cone-only, RTD-only, and combined ablations on identical paired data | Metrics are redundant in prediction and intervention | **Untested.** No direct learned-map cone loss was present historically. The exact-chain-map and soft-cone components make a future controlled comparison possible, but they are not integrated into the graph/cell/sheaf translators and no cone-only/RTD-only/combined result exists. |
| C5 | Sheaf routes are selected only when local compatibility matters | Neural Sheaf Diffusion, Knowledge Sheaves | Controlled local-consistency benchmark | Sheaf selection unrelated to regime or utility | **Exploratory only.** Run 9 selected sheaf on 51% of sheaf-regime examples versus 17–26% elsewhere, but the router target was distilled from a validation regime-by-route table. This supports a scoped behavior description, not the stronger causal or input-only-utility claim. |
| C6 | Chemically valid ring/2-cell lifts improve molecular property prediction over the graph route | Differentiable liftings, TopoBench | OGBG-MOLHIV official scaffold split and evaluator, matched configs, multiple seeds | Graph route matches or beats the ring-lift route | **Exploratory negative.** v1 cell scored 0.723 ± 0.017 versus graph 0.771 ± 0.014. The v2 architecture reached 0.757 ± 0.002, but it was designed after inspecting the same official test split, and v3 reused it again. These are development observations, not a held-out validation of the redesign; a fresh benchmark/task or nested protocol is required. |

## Claim discipline

- Diagnostic correlation is not causal evidence that a loss improves training.
- Improvement over one fixed graph model is not evidence that dynamic routing is necessary.
- A lower topological defect is not evidence of greater semantic information.
- A synthetic result is not evidence of universal representation efficiency.
- “First” claims require a refreshed systematic search immediately before submission.
