# Claims ledger

Every proposed claim must identify its closest prior art, supporting experiment, falsifier, and current status.

| ID | Proposed claim | Closest prior art | Required evidence | Falsifier | Status |
|---|---|---|---|---|---|
| C1 | Structural defects predict task damage during typed conversions | RTD, RTD-AE, SRTD | Held-out correlation and intervention tests beyond reconstruction baselines | No incremental predictive value | **Conversion claim untested; the available diagnostics are null.** Both corruption families evaluate fixed-expert clean/corrupted embeddings, never a translator or learned map. Each kind has 13 complete blocks, 65 batch observations, and 306 unique examples. Gate-3 base: all nine paired contrasts against task-only include zero (p≥0.324), conditional on fixed checkpoints. Gauge: eight seed-matched pairs raise the unit of analysis to the training seed, and all three across-seed Student-t intervals (df=7) include zero with exact sign tests p≥0.727. No multiplicity adjustment anywhere. These diagnostics still do not test damage during typed conversion. |
| C2 | Map-aware cone loss improves a learned graph/cell/sheaf translator | RTD-AE, differentiable liftings | Matched-compute ablations across synthetic regimes and a real task | Task/cycle-only model matches it | **Unsupported on the one benchmark that can now test it; the translator claim remains untested.** The identifiable campaign runs a direct learned-map cone ablation: adding `cone` to a working `task_reconstruction` objective moved nothing, with all declared contrast intervals containing zero. The falsifier fired — the task-only-plus-reconstruction model matches the cone-augmented model exactly. This is a weak null because both saturate the accuracy ceiling. Historical surrogate ablations also showed no benefit and consumed target-view structure. No real-task or graph-to-cell/sheaf translator ablation exists. |
| C3 | A cost-aware router specializes by structural regime | MoE routing, learned topological liftings | Regime-conditioned route analysis and fixed/random/oracle comparisons | Route collapse or shortcut behavior | **Scoped routing endpoint supported under the v2 protocol.** Across five valid seeds, hard-minus-best-fixed was +0.1098 (SD 0.0117; Student-t 95% CI [0.0953, 0.1243]); all margins were positive. This is the historical regime-distilled router: training used privileged regime labels and inference used graph, active-face, and sheaf-transport summaries. It is not graph-only conversion. The campaign is protocol-aligned, not pristine preregistration, because an aborted pre-commit seed-20260906 attempt exposed validation metrics under different code. With n=5, assumptions are uncheckable and the exact two-sided sign-test sensitivity is p=0.0625. |
| C7b | An exactness constraint derived from the input is a strong prior on a learned conversion, and its violation measures the damage | Structural regularisation; RTD/SRTD | Preregistered paired campaign with a multiplicity-adjusted decision rule and a matched-rank control | Generic shrinkage or a random constraint of equal rank matches it | **Supported, confirmatorily.** `docs/27` froze the protocol before the run; `docs/28` reports it. Exactness improves a learned conversion with Bonferroni-adjusted interval [−2.802, −1.458] on `log10` held-out error, roughly a hundred- to thousand-fold. The falsifier did not fire: ridge at its best of four weights buys ~2.5× and a random subspace of rank matched to `B1ᵀ` buys nothing. **C1 for this conversion is supported**: the within-topology correlation between a learned map's exactness violation and its held-out error is +0.854, CI [+0.831, +0.877], positive in 29/29 topologies. Boundary: one synthetic family, linear conversions, and the mechanism is understood — `W B1ᵀ = 0` forces each row into the cycle space. |
| C4 | Direct cone loss supplies information not contained in RTD | RTD/SRTD | Cone-only, RTD-only, and combined ablations on identical paired data | Metrics are redundant in prediction and intervention | **Refuted confirmatorily; both fail as training terms.** In the preregistered conversion campaign (`docs/28`) the cone term *harms*, adjusted interval [+0.102, +0.277], and RTD is inert, [−0.003, +0.039]. Neither supplies information the other lacks because neither supplies usable information at all. Across two campaign families the cone has now failed for two distinct reasons: constant over a fixed candidate class, and violated by the ground truth in the learned-conversion setting. **Both are retained as diagnostics only and should not be tested as losses again without first passing `screen_structural_term`.** Earlier exploratory status follows. **Tested and unsupported.** The required ablations now exist on identical paired data: `cone_only`, `rtd_only`, and `combined`, five seeds each. Neither term identifies the map alone — transformation accuracy 0.0815 (cone) and 0.0833 (RTD) against a 0.0833 chance baseline — and adding either or both to a working objective changed nothing. On this benchmark the two metrics are equally uninformative for identification rather than complementary, and the reason is structural rather than empirical: every basis map is a signed permutation, hence an invertible isometry, so cone acyclicity and RTD are both **constant** on the hypothesis space and carry exactly zero information about which element was planted. `cone_only` and `rtd_only` produce acyclic cones in 6,000 of 6,000 evaluated examples while identifying at chance. Cone acyclicity certifies invertibility, not correctness, and the same degeneracy afflicts any hypothesis class of invertible maps. |
| C3b | A router can select a view by measuring what converting into it costs | MoE routing; structural diagnostics | Out-of-sample threshold on the measured defect, compared against the better fixed strategy | Routing is not distinguishable from a single fixed view | **Not supported.** Preregistered in `docs/27`, reported in `docs/28`: interval [−0.111, +0.382] contains zero, and a clearly post hoc comparison against always-cell also contains zero. The router picks the better view on 25/28 trials, so the defect does carry signal about *which* view; the habitat gave it nothing to win, because a perfect oracle buys only 1.36× over always-cell. An exploratory version that appeared to beat both fixed strategies used an in-sample threshold and is withdrawn. **The surviving routing claim is about compute, not accuracy**: routed inference is 1.532 ± 0.035× faster than dense three-expert evaluation and 2.269 ± 0.043× slower than the fastest single fixed route. |
| C5 | Sheaf routes are selected only when local compatibility matters | Neural Sheaf Diffusion, Knowledge Sheaves | Controlled local-consistency benchmark | Sheaf selection unrelated to regime or utility | **Exploratory only.** Run 9 selected sheaf on 51% of sheaf-regime examples versus 17–26% elsewhere, but the router target was distilled from a validation regime-by-route table. This supports a scoped behavior description, not the stronger causal or input-only-utility claim. |
| C6 | Chemically valid ring/2-cell lifts improve molecular property prediction over the graph route | Differentiable liftings, TopoBench | OGBG-MOLHIV official scaffold split and evaluator, matched configs, multiple seeds | Graph route matches or beats the ring-lift route | **Exploratory negative.** v1 cell scored 0.723 ± 0.017 versus graph 0.771 ± 0.014. The v2 architecture reached 0.757 ± 0.002, but it was designed after inspecting the same official test split, and v3 reused it again. These are development observations, not a held-out validation of the redesign; a fresh benchmark/task or nested protocol is required. |

| C7 | A learned system can recover an exact typed chain map when the correct map lies in a known finite family | Exact chain-map constructions; differentiable liftings | Frozen factorial campaign on an identifiable benchmark with an absolute recovery gate, chance and analytic baselines, and exact cone evaluation | Recovery gate fails, or accuracy does not exceed chance | **Completed and supported, within a narrow boundary.** The 40-run frozen campaign (8 ablations × 5 seeds) on one six-sector cellular annulus recovers the planted dihedral map exactly: transformation and cell-face accuracy 1.000 with map MSE at 1e-16 in every objective carrying task or reconstruction supervision, and the engineering recovery gate passed 10/10 applicable runs. The boundary is real: an analytic marker decoder also reaches 1.000, so the ceiling was attainable without learning; the model is a flattened MLP selecting from a hard-coded twelve-element basis, not a general graph network; and the verified identity is the chain-map law to a fixed 1e-5 tolerance on one synthetic template. Full record in `docs/23-identifiable-results.md`. |

## Claim discipline

- Diagnostic correlation is not causal evidence that a loss improves training.
- Improvement over one fixed graph model is not evidence that dynamic routing is necessary.
- A lower topological defect is not evidence of greater semantic information.
- A synthetic result is not evidence of universal representation efficiency.
- “First” claims require a refreshed systematic search immediately before submission.
