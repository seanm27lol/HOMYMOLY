# Claims ledger

Every proposed claim must identify its closest prior art, supporting experiment, falsifier, and current status.

| ID | Proposed claim | Closest prior art | Required evidence | Falsifier | Status |
|---|---|---|---|---|---|
| C1 | Structural defects predict task damage during typed conversions | RTD, RTD-AE, SRTD | Held-out correlation and intervention tests beyond reconstruction baselines | No incremental predictive value | **A scoped regularization-path association is supported; the broad claim is not.** Along the prespecified nine-weight compatibility-penalty path, the boundary-compatibility defect covaries positively with held-out error within each topology: the secondary Pearson summary is positive in 29/29 topologies and its unadjusted 95% interval excludes zero. The fits reuse the same data and initialization, `lambda` is a common driver, and the nine points are not independent observations. This is not independent predictive information, off-path or unseen-topology calibration, sequence exactness, an isolated intervention, or causal evidence. Separately, both corruption families evaluate fixed-expert embeddings rather than a translator; their twelve reported intervals contain zero and do not test conversion damage. |
| C2 | Map-aware cone loss improves a learned graph/cell/sheaf translator | RTD-AE, differentiable liftings | Matched-compute ablations across synthetic regimes and a real task | Task/cycle-only model matches it | **Unsupported in the annulus benchmark; the broad translator claim remains untested.** Adding the differentiable annulus cone proxy to a saturated task-plus-reconstruction objective changed nothing. Exact hard-map cone acyclicity cannot distinguish the twelve decoded dihedral maps, but the soft-mixture training proxy can vary, so this is not a theorem that the optimization signal is constant. The continuous conversion campaign tested a singular-value surrogate, not mapping-cone homology. No real-task graph-to-cell/sheaf translator ablation exists. |
| C3 | A cost-aware router specializes by structural regime | MoE routing, learned topological liftings | Regime-conditioned route analysis and fixed/random/oracle comparisons | Route collapse or shortcut behavior | **Scoped descriptive endpoint supported under the v2 rule.** Across five valid seeds, hard-minus-best-fixed was +0.1098 (SD 0.0117; Student-t 95% CI [0.0953, 0.1243]); all margins were positive. Training used privileged regime labels and inference used graph, active-face, and sheaf-transport summaries. This is not graph-only conversion or part of the H1–H3 multiplicity family. The campaign is protocol-aligned, not pristine preregistration, because an aborted pre-commit seed-20260906 attempt exposed validation metrics under different code. With n=5, assumptions are uncheckable and the exact two-sided sign-test sensitivity is p=0.0625. |
| C7b | An input-derived boundary-compatibility penalty improves a learned conversion, and its defect covaries with held-out error along a penalty path | Structural regularisation; RTD/SRTD | Prospectively locked same-family paired campaign with a multiplicity-adjusted primary decision rule and machine-verifiable results | The compatibility penalty does not beat the unpenalized fit | **Supported within one synthetic linear family, subject to outcome-informed design and a protocol implementation deviation.** `docs/27` froze the analysis before the run, but the hypotheses, directions, endpoint, training size, and weights were selected after same-family exploration; because exploratory seed identities were not retained, overlap with the frozen seed block is unverifiable. This is not independent confirmation. The protocol specified the Frobenius sum while the runner executed `mean((B1 Wᵀ)²)`; `docs/28` discloses this. The primary adjusted interval for the executed compatibility penalty lies below zero. The key `exact` is historical shorthand: `B1 Wᵀ = 0` establishes `d∘d = 0`, not `im Wᵀ = ker B1`. C1 is an unadjusted secondary regularization-path association, not evidence of independent prediction. Specificity against tuned ridge or a rank-matched random subspace remains open because the exploratory control values lack a generating script and machine result. |
| C4 | Direct cone loss supplies information not contained in RTD | RTD/SRTD | Cone-only, RTD-only, and combined ablations on identical paired data | Metrics are redundant in prediction and intervention | **Not resolved for mapping-cone homology versus published RTD.** The continuous campaign tested narrower surrogates: the singular-value cone surrogate harms at the tested weight, while the RTD-inspired distance surrogate shows no detected improvement; neither result transfers to the named mathematical methods. In the annulus campaign, `cone_only` and `rtd_only` identify at chance and added losses do not improve a saturated control. All hard decoded maps have acyclic cones, but `cone_soft_betti` varies over soft mixtures. The RTD-style loss also varies, compares predicted with target batch geometry, and is not an unsupervised or constant control. |
| C3b | A router can select a view by measuring what converting into it costs | MoE routing; structural diagnostics | Out-of-sample threshold on the measured defect, compared against the better fixed strategy | Routing is not distinguishable from a single fixed view | **Untested by the frozen H5 decision.** Its numerator selects one of the cell/graph errors while its denominator is their per-trial minimum, forcing every endpoint observation to be nonnegative; support required an interval below zero and was impossible. The 25/28 route-selection rate and a post hoc always-cell contrast are descriptive only. A new endpoint is required. The separate compute result remains: routed inference is 1.532 ± 0.035× faster than dense three-expert evaluation and 2.269 ± 0.043× slower than the fastest fixed route. |
| C5 | Sheaf routes are selected only when local compatibility matters | Neural Sheaf Diffusion, Knowledge Sheaves | Controlled local-consistency benchmark | Sheaf selection unrelated to regime or utility | **Exploratory only.** Run 9 selected sheaf on 51% of sheaf-regime examples versus 17–26% elsewhere, but the router target was distilled from a validation regime-by-route table. This supports a scoped behavior description, not the stronger causal or input-only-utility claim. |
| C6 | Chemically valid ring/2-cell lifts improve molecular property prediction over the graph route | Differentiable liftings, TopoBench | OGBG-MOLHIV official scaffold split and evaluator, matched configs, multiple seeds | Graph route matches or beats the ring-lift route | **Exploratory negative.** v1 cell scored 0.723 ± 0.017 versus graph 0.771 ± 0.014. The v2 architecture reached 0.757 ± 0.002, but it was designed after inspecting the same official test split, and v3 reused it again. These are development observations, not a held-out validation of the redesign; a fresh benchmark/task or nested protocol is required. |

| C7 | A learned system can recover an exact typed chain map when the correct map lies in a known finite family | Exact chain-map constructions; differentiable liftings | Frozen factorial campaign on an identifiable benchmark with an absolute recovery gate, chance and analytic baselines, and exact cone evaluation | Recovery gate fails, or accuracy does not exceed chance | **Completed and supported, within a narrow boundary.** The 40-run frozen campaign (8 ablations × 5 seeds) on one six-sector cellular annulus recovers the planted dihedral map exactly: transformation and cell-face accuracy 1.000 with map MSE at 1e-16 in every objective carrying task or reconstruction supervision, and the engineering recovery gate passed 10/10 applicable runs. The boundary is real: an analytic marker decoder also reaches 1.000, so the ceiling was attainable without learning; the model is a flattened MLP selecting from a hard-coded twelve-element basis, not a general graph network; and the verified identity is the chain-map law to a fixed 1e-5 tolerance on one synthetic template. Full record in `docs/23-identifiable-results.md`. |

## Claim discipline

- Diagnostic correlation is not causal evidence that a loss improves training.
- The boundary law `d∘d = 0` is not sequence exactness `im d_(n+1) = ker d_n`.
- Frozen campaign keys do not license stronger names: `cone` and `rtd` are
  surrogates unless the exact named constructions are computed.
- Improvement over one fixed graph model is not evidence that dynamic routing is necessary.
- A lower topological defect is not evidence of greater semantic information.
- A synthetic result is not evidence of universal representation efficiency.
- “First” claims require a refreshed systematic search immediately before submission.
